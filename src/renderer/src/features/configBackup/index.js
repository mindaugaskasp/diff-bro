// Back up and restore the whole configuration — snippets, vault, identity keys
// and settings — as one passphrase-sealed archive.
export { useConfigBackupStore } from './configBackupStore'
export { default as ConfigBackupDialog } from './components/ConfigBackupDialog.vue'
