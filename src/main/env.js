// Set only by `npm run dev` — its presence distinguishes dev from a packaged
// build (the kill switch and will-navigate block key off it).
export const DEV_URL = process.env['ELECTRON_RENDERER_URL']
