node_modules/
build/
dist/
coverage/
test-results/
# Traces copied out of test-results/ before the next run wipes it (see `make e2e`).
e2e-failures/
playwright-report/
.claude/settings.local.json
# Agent worktrees are whole checkouts of this repo nested inside it.
.claude/worktrees/
# `make local-seed` fixtures — generated, and the streamed pair alone is 80 MB.
seed-files/
.DS_Store
*.log
*~
quality-audit.md
